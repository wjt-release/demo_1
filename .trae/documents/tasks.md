# Implementation Tasks

- [ ] **1. Project Initialization**
    - [ ] Initialize Vite project with React and TypeScript.
    - [ ] Install dependencies: `react-router-dom`, `zustand`, `lucide-react`, `framer-motion`, `clsx`, `tailwind-merge`.
    - [ ] Configure Tailwind CSS.
    - [ ] Set up project folder structure.

- [ ] **2. Core Components & Layout**
    - [ ] Create `Button`, `Input`, `Modal` components.
    - [ ] Create `Header` (Navigation, Search, Cart Icon).
    - [ ] Create `Footer`.
    - [ ] Create `Layout` component.
    - [ ] Set up Routes in `App.tsx`.

- [ ] **3. Data Layer & State Management**
    - [ ] Create mock data (products, users).
    - [ ] Set up Zustand store (`useCartStore`, `useUserStore`, `useProductStore`).
    - [ ] Implement mock API services.

- [ ] **4. Feature Implementation: Home & Products**
    - [ ] Build `HomePage` (Hero Carousel, New Arrivals).
    - [ ] Build `ProductListPage` (Grid, Filter).
    - [ ] Build `ProductDetailPage` (Gallery, Info, Add to Cart).

- [ ] **5. Feature Implementation: Shopping Flow**
    - [ ] Build `CartPage`.
    - [ ] Build `CheckoutPage` (Address Form, Payment).
    - [ ] Implement Mock Payment logic.

- [ ] **6. Feature Implementation: User & Orders**
    - [ ] Build `AuthPage` (Login/Register).
    - [ ] Build `OrderHistoryPage`.
    - [ ] Build `ContactPage`.

- [ ] **7. Polish & Refinement**
    - [ ] Add animations with Framer Motion.
    - [ ] Ensure mobile responsiveness.
    - [ ] Verify "Coming Soon" states.
